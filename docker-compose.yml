services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: rainfrog
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - rainfrog_postgres:/var/lib/postgresql/data

volumes:
  rainfrog_postgres:
